---
description: Initialize an LLM Wiki (directory structure, schema, index, log, backlog)
argument-hint: "[wiki_path]"
allowed-tools: Bash(python3 "${CLAUDE_PLUGIN_ROOT}/scripts/wiki-tools.py":*), Read, Write
---

Initialize a new LLM Wiki using the `jx-kb:init` skill.

$ARGUMENTS
