---
description: Clean up a worktree by committing all modified and untracked files, merging changes back to main, and removing the worktree
argument-hint: ""
allowed-tools: Read
---

Clean up a worktree by committing all modified and untracked files, merging changes back to main, and removing the worktree using the `jx-pm:clean-worktree` skill.

$ARGUMENTS
