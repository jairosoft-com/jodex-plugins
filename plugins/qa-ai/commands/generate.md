---
description: Generate Playwright TypeScript spec files from an xlsx test plan by live-exploring the site
argument-hint: "[xlsx_path]"
allowed-tools: Bash(playwright-cli:*), Bash(npx playwright test:*), Bash(python3 "${CLAUDE_PLUGIN_ROOT}/scripts/xlsx-writer.py":*), Bash(ls:*), Read, Write
---

Generate Playwright test specs from the test plan using the `qa-ai:generate` skill.

$ARGUMENTS
