# Scripts

Pinned helper scripts used by commands and skills. Keep permissions narrow and prefer stdlib-only helpers when practical.

- `skill-creator.py` — validation, collision checking, and atomic scaffolding for `/jx-plugin:create-skill`
- `plugin-creator.py` — validation, collision checking, and atomic scaffolding for `/jx-plugin:create-plugin`
